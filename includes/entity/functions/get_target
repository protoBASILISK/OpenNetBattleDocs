<h3 id='get_target'>: get_target()</h3><div class="highlight"><pre class="highlight lua tab-lua"><code><span class="kd">local</span>   <span class="p">;</span><span class="c1">-- example </span>
<span class="kd">local</span> <span class="n">_target</span> <span class="o">=</span>     <span class="p">:</span><span class="n">get_target</span><span class="p">()</span>
</code></pre></div>
<p>Returns a <strong><code>pointer</code></strong> to the  that this  is targeting.
TODO figure out what exactly sets/removes targets</p>
<h4 id='function-call'>Function Call</h4>
<p><code>:get_target()</code></p>
<h4 id='return-value'>Return Value</h4>
<p>TODO probably </p>
