<h3 id='field-get_character'>Field : get_character()</h3><div class="highlight"><pre class="highlight lua tab-lua"><code><span class="kd">local</span> <span class="n">example_id</span>    <span class="c1">-- The ID of an example character</span>
<span class="kd">local</span> <span class="n">example_character</span> <span class="o">=</span> <span class="n">field</span><span class="p">:</span><span class="n">get_character</span><span class="p">(</span> <span class="n">character_id</span> <span class="p">)</span>
</code></pre></div>
<p>This function returns a <strong><code>pointer</code></strong> to a  with a specific ID.</p>

<p>If there is no  with the provided ID, <code>nil</code> is returned instead.<br>
This also happens if there is an  with the provided ID, but is <em>not</em> a  (e.g. is an , etc.)</p>
<h4 id='function-call'>Function Call</h4>
<p><code>field:get_character( character_id )</code></p>
<h4 id='return-value'>Return Value</h4><h4 id='parameters'>Parameters</h4>
<p><strong><code>integer</code></strong> <code>character_id</code><br>
    &emsp;<em>The ID of the  you want to retrieve.</em></p>
