<% @current_callback =
    {
        callback_name: "battle_end_func",
        lua_code:
"local " + @variable_text + " -- example " + @variable_text + "

" + @variable_text + ".battle_end_func = function( actor )
    print( \"I LIVED!\" )
end",
        description: "The function that will be called if the " + @class_name + " is alive when the battle ends.<br>",
        parameters: [
            {
                name: "actor",
                desc: "The " + @class_name + ".",
                type: @class_name
            }
        ],
        return_value_type: "nil",
    }
%>