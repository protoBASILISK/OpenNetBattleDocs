<% @current_callback =
    {
        callback_name: "special_attack_func",
        lua_code:
"local " + @variable_text + " -- example " + @variable_text + "

" + @variable_text + ".special_attack_func = function( actor )
    return Battle.Buster.new( actor, true, 10 )
end",
        description: "The function that will be called when the " + @class_name + " uses a **special** attack.",
        parameters: [
            {
                name: "actor",
                desc: "The " + @class_name + " that will be executing the " + @link_card_action + ".",
                type: @class_name
            },
        ],
        return_value_type: @link_card_action,
        return_value_desc: "The " + @link_card_action + " that will be executed for the **special** attack.",
    }
%>