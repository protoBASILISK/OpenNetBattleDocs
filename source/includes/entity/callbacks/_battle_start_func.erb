<% @current_callback =
    {
        callback_name: "battle_start_func",
        lua_code:
"local " + @variable_text + " -- example " + @variable_text + "

" + @variable_text + ".battle_start_func = function( actor )
    print( \"battle begin.\" )
end",
        description: "The function that will be called if the the " + @class_name + " is alive at the start of battle.",
        parameters: [
            {
                name: "actor",
                desc: "The " + @class_name + ".",
                type: @class_name
            }
        ],
        return_value_type: "nil",
    }
%>