### <%= @class_header %> . battle_end_func
``` lua
local <%= @variable_text %>	;-- example <%= @variable_text %>
local function on_battle_end()
end

<%= @variable_text %>.battle_end_func = on_battle_end
```

Assigns a **`function`** that will be executed when the battle ends if this <%= @class_name %> is present on the battlefield.

#### Variable Type
**`function`**

#### Assignment
`<%= @variable_text %>.battle_end_func = on_battle_end`

#### Parameters
**`function`** `on_battle_end`<br>
	&emsp;_The **`function`** you want this <%= @class_name %> to execute when the battle ends._<br>
	
``` lua
local function battle_end_func( self )
end
```
<aside class="notice"><code>battle_end_func</code> requires <b>one</b> <%= @class_name %> parameter, and to return <b>nothing</b>.<br>
The <code>self</code> parameter will be passed a reference to itself.</aside>
