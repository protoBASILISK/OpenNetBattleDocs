<% @current_function =
    {
        function_name: "never_flip",
        lua_code:
"local " + @variable_text	+ " -- example " + @variable_text + "
" + @variable_text + ":never_flip( true ) ",
        description: "If `true`, stops this " + @class_name + " from flipping direction (graphically) based on its " + @link_team + ".",
        parameters: [
            {
                name: "disable_flip",
                desc: "`true`, if this " + @class_name + " should not flip direction (graphically).",
                type: "boolean"
            }
        ]
    }
%>