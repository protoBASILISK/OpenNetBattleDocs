### <%= @class_header %> : will_remove_eof()
``` lua
local <%= @variable_text %>	;-- example <%= @variable_text %>
local will_remove = 	;<%= @variable_text %>:will_remove_eof()
```

Queries whether or not this <%= @class_name %> will be removed at the end of the frame.<br>
Returns `true` if they will be deleted, otherwise `false`.

#### Function Call
`<%= @variable_text %>:will_remove_eof()`

#### Return Value
**`boolean`**
