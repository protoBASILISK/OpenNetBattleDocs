### <%= @class_header %> . battle_start_func
``` lua
local <%= @variable_text %>	;-- example <%= @variable_text %>
local function on_battle_start()
start

<%= @variable_text %>.battle_start_func = on_battle_start
```

Assigns a **`function`** that will be executed when the battle starts if this <%= @class_name %> is present on the battlefield.

#### Variable Type
**`function`**

#### Assignment
`<%= @variable_text %>.battle_start_func = on_battle_start`

#### Parameters
**`function`** `on_battle_start`<br>
	&emsp;_The **`function`** you want this <%= @class_name %> to execute when the battle starts._<br>
	
``` lua
local function battle_start_func( self )
start
```
<aside class="notice"><code>battle_start_func</code> requires <b>one</b> <%= @class_name %> parameter, and to return <b>nothing</b>.<br>
The <code>self</code> parameter will be passed a reference to itself.</aside>
