<% @current_function =
    {
        function_name: "is_moving",
        lua_code:
"local " + @variable_text	+ " -- example " + @variable_text + "
local is_moving = " + @variable_text + ":is_moving()",
        description: "Returns `true` if this " + @class_name + " is currently moving in any fashion.<br>" +
                        "&emsp;Otherwise, returns `false`.",
        return_value_type: "boolean",
        return_value_desc: "Whether this " + @class_name + " is currently moving or not."
    }
%>