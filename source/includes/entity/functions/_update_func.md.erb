### <%= @class_header %> . update_func
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
local number_prints = 0

local function on_update( self, time )
  number_prints = number_prints + 1
  print( number_prints )
end

local <%= @variable_text %>.update_func = on_update
```

Assigns a **`function`** that will be executed every frame that this <%= @class_name %> is alive for.

#### Variable Type
**`function`**

#### Assignment
`<%= @variable_text %>.update_func = on_update`

#### Parameters
**`function`** `on_update`<br>
	&emsp;_The **`function`** you want this <%= @class_name %> to execute every frame._<br>
	
``` lua
local function update_func( self, elapsed_time )
end
```
<aside class="notice"><code>update_func</code> requires <b>one</b> <%= @class_name %> parameter, <b>one</b> <b><code>number</code></b> parameter, and to return <b>nothing</b>.<br>
The <code>self</code> parameter will be passed a reference to itself.<br>
The <code>number</code> parameter is the <i>elapsed time</i> since the last time <code>update_func</code> was called for this <%= @class_name %>.</aside>
