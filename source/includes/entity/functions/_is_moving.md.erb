### <%= @class_header %> : is_moving()
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
local is_<%= @variable_text %>_moving =	 	<%= @variable_text %>:is_moving()
```

Queries whether or not this <%= @class_name %> is currently *moving around in any way*.<br>
Returns `true` if they are, otherwise `false`.

#### Function Call
`<%= @variable_text %>:is_moving()`

#### Return Value
**`boolean`**
