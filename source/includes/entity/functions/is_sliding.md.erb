### <%= @class_header %> : is_sliding()
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
local is_<%= @variable_text %>_sliding = 	<%= @variable_text %>:is_sliding()
```

Queries whether or not this <%= @class_name %> is currently *sliding*.<br>
Returns `true` if they are, otherwise `false`.

#### Function Call
`<%= @variable_text %>:is_sliding()`

#### Return Value
**`boolean`**
