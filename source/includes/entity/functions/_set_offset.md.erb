### <%= @class_header %> : set_offset()
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
<%= @variable_text %>:set_offset( 100, 50 )
```
> Changes the **offset** of this <%= @class_name %> to be **`100`** pixels to the right, and **`50`** pixels down.

The **offset** of a <%= @class_name %> determines where on the grid it will be drawn.<br>
    &emsp;**Positive** X values move it to the *right*, negative X values move it to the *left*.<br>
    &emsp;**Positive** Y values move it *downwards*, negative Y values move it *upwards*.

#### Function Call
`<%= @variable_text %>:set_elevation( elevation )`

#### Parameters
**`integer`** `X`<br>
    &emsp;_The *horizontal offset* of this <%= @class_name %>._<br>

**`integer`** `Y`<br>
    &emsp;_The *vertical offset* of this <%= @class_name %>._