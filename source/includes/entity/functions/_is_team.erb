<% @current_function =
    {
        version: "2.0",
        function_name: "is_team",
        lua_code:
"local " + @variable_text	+ " -- example " + @variable_text + "
local is_red = " + @variable_text + ":is_team( Team.Red )",
        description: "Returns `true` if this " + @class_name + " is a member of the provided " + @link_team + ".<br>" +
                        "&emsp;Otherwise, returns `false`.",
        return_value_type: @link_team,
        return_value_desc: "The " + @link_team + " of this " + @class_name + "."
    }
%>