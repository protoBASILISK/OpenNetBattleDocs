### <%= @class_header %> : will_erase_eof()
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
local will_erase = 	<%= @variable_text %>:will_erase_eof()
```

Queries whether or not this <%= @class_name %> will be erased at the end of the frame.<br>
Returns `true` if they will be deleted, otherwise `false`.

#### Function Call
`<%= @variable_text %>:will_erase_eof()`

#### Return Value
**`boolean`**
