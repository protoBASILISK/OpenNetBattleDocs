<% @current_function =
    {
        version: "2.0",
        function_name: "is_teleporting",
        lua_code:
"local " + @variable_text	+ " -- example " + @variable_text + "
local is_teleporting = " + @variable_text + ":is_teleporting()",
        description: "Returns `true` if this " + @class_name + " is currently teleporting.<br>" +
                        "&emsp;Otherwise, returns `false`.",
        return_value_type: "boolean",
        return_value_desc: "Whether this " + @class_name + " is teleporting or not."
    }
%>