### <%= @class_header %> . attack_func
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
local function on_attack( self, target )
end

<%= @variable_text %>.attack_func = on_attack
```

Assigns a **`function`** that will be executed when this <%= @class_name %> **damages** another <%= @link_entity %>.

#### Variable Type
**`function`**

#### Assignment
`<%= @variable_text %>.attack_func = on_attack`

#### Parameters
**`function`** `on_attack`<br>
	&emsp;_The **`function`** you want this <%= @class_name %> to execute when it successfully this a <%= @link_character %>._<br>

``` lua
local function attack_func( self, hit_character )
end
```
<aside class="notice"><code>attack_func</code> requires <b>one</b> <%= @class_name %> parameter, <b>one</b> <%= @link_character %> parameter, and to return <b>nothing</b>.<br>
The <code>self</code> parameter will be passed a reference to itself.<br>
The <code>hit_character</code> parameter will be passed a reference to the <%= @link_character %> it hit.</aside>
