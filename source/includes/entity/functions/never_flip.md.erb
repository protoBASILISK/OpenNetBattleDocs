### <%= @class_header %> : never_flip()
``` lua
local <%= @variable_text %>	;-- example <%= @variable_text %>
<%= @variable_text %>:never_flip( true )
```

#### Function Call
`<%= @variable_text %>:never_flip( should_never_flip )`

#### Parameters
**`boolean`** `should_never_flip`<br>
	&emsp;_`true` if you want this <%= @class_name %> to always face the same direction. Otherwise, `false`._
