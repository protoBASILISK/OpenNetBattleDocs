<% @current_function =
    {
        function_name: "add_defense_rule",
        lua_code:
"local " + @variable_text	+ " -- example " + @variable_text + "
local example_rule  -- example DefenseRule
" + @variable_text + ":add_defense_rule( example_rule ) ",
        description: "Adds a " + @link_defense_rule + " to a(n) " + @class_name + ".<br>" +
                        @link_defense_rule + "s allow the " + @class_name + " to change/ignore certain properties of attacks that affect them.<br>" +
                        "**TODO** : Get an actually good explanation of what they do.",
        parameters: [
            {
                name: "defense_rule",
                desc: "**TODO**",
                type: @link_defense_rule
            }
        ]
    }
%>