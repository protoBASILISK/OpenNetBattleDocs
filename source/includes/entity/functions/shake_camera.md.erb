### <%= @class_header %> : shake_camera()
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
<%= @variable_text %>:shake_camera( 1.0, 1.0 )
```

Causes the <%= @class_name %> to shake the camera, at a specified for a specified time.

#### Function Call
`<%= @variable_text %>:shake_camera( power, duration )`

#### Parameters
**`number`** `power`<br>
	&emsp;_How **pronounced** the shaking should be._<br>
**`number`** `duration`<br>
	&emsp;_The time (**in seconds**) the shaking should last._
