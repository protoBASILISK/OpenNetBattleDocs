### <%= @class_header %> . on_spawn_func
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
local function on_spawn( self, spawn_tile )
end

<%= @variable_text %>.on_spawn_func = on_spawn
```

Assigns a **`function`** that will be executed when this <%= @class_name %> is spawned into the game field.

#### Variable Type
**`function`**

#### Assignment
`<%= @variable_text %>.on_spawn_func = on_spawn`

#### Parameters
**`function`** `on_spawn`<br>
	&emsp;_The **`function`** you want this <%= @class_name %> to execute when it is first spawned._<br>
	
``` lua
local function on_spawn_func( self, spawn_tile )
end
```
<aside class="notice"><code>on_spawn_func</code> requires <b>one</b> <%= @class_name %> parameter, <b>one</b> <%= @link_tile %> parameter, and to return <b>nothing</b>.<br>
The <code>self</code> parameter will be passed a reference to itself.<br>
The <code>spawn_tile</code> parameter will be passed a reference to the <%= @link_tile %> it spawns on.</aside>
