### <%= @class_header %> : get_height()
``` lua
local height = <%= @variable_text %>:set_height( 100 )
```

The **height** of a <%= @class_name %> determines where various elements will show up.<br>
    &emsp;*e.g. Held cards, impact graphics, etc.*

#### Function Call
`<%= @variable_text %>:get_height()`

#### Return Value
**`integer`** `height`<br>
	&emsp;_The *height* of this <%= @class_name %> (in pixels)._