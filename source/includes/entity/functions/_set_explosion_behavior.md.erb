### <%= @class_header %> : set_explosion_behavior()
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
<%= @variable_text %>:set_explosion_behavior( 4, 1.0, false )
```

Defines the behaviour of the explosions that happen when the <%= @class_name %> is destroyed.

#### Function Call
`<%= @variable_text %>:set_explosion_behavior( count, speed, is_boss )`

#### Parameters
**`integer`** `count`<br>
	&emsp;_How many explosions happen when the <%= @class_name %> is destroyed._<br>
**`number`** `speed`<br>
	&emsp;_The speed at which the animations play._<br>
**`boolean`** `is_boss`<br>
	&emsp;_Whether this <%= @class_name %> is considered a **boss** or not._<br>
