<% @current_function =
    {
        version: "2.0",
        function_name: "get_max_health",
        lua_code:
"local " + @variable_text	+ " -- example " + @variable_text + "
local " + @variable_text + "_max_health = " + @variable_text + ":get_max_health()",
        description: "Returns the **natural** maximum HP of this " + @class_name + ", without modifiers.",
        return_value_type: "number",
        return_value_desc: "The **natural** maximum HP of this " + @class_name + "."
    }
%>