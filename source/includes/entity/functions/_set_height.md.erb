### <%= @class_header %> : set_height()
``` lua
local <%= @variable_text %>	-- example <%= @variable_text %>
<%= @variable_text %>:set_height( 100 )
```
> Changes the **height** of this <%= @class_name %> to be **`100`** pixels.

The **height** of a <%= @class_name %> determines where various elements will show up.<br>
    &emsp;*e.g. Held cards, impact graphics, etc.*

#### Function Call
`<%= @variable_text %>:set_height( height )`

#### Parameters
**`integer`** `height`<br>
	&emsp;_The *height* you want this <%= @class_name %> to be (in pixels)._