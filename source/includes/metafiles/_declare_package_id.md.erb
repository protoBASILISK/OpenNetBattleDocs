### <%= @class_header %> : declare_package_id()
``` lua
meta:declare_package_id( "com.example.<%= @variable_text %>.test" )
```
This function assigns the *package name* for the <%= @meta_class %>.<br>
*Package names* are used as *unique identifiers*, and should be named in a way that will minimize conflicts with other mods.<br>
(*e.g. using a project name: `com.bn1_is_back_baby.<%= @variable_text %>.example`*)

#### Function Call
`meta:declare_package_id( package_name )`

#### Parameters
**`string`** `package_name`<br>
&emsp;_A **unique package name** to identify this mod with._
