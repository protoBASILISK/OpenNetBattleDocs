### <%= @class_header %> : set_emotions_texture_path()
``` lua
meta:set_emotions_texture_path( _modpath.."emotions.png" )
```
This function assigns the *graphics* that will be displayed in battle for the <%= @class_name %>'s *emotion window* at the top corner of the screen.

#### Function Call
`meta:set_emotions_texture_path( path )`

#### Parameters
**`string`** `path`<br>
&emsp;_The **relative path** from the executable to the file._
