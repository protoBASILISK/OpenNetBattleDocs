### <%= @class_header %> : set_mugshot_texture_path()
``` lua
meta:set_mugshot_texture_path( _modpath.."mugshot.png" )
```
This function assigns the *graphics* that will be displayed for the <%= @class_name %>'s face while they are talking in a text box.

#### Function Call
`meta:set_mugshot_texture_path( path )`

#### Parameters
**`string`** `path`<br>
&emsp;_The **relative path** from the executable to the file._
