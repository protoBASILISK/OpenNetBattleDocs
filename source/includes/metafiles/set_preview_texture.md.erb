### <%= @class_header %> : set_preview_texture()
``` lua
meta:set_preview_texture( _modpath.."preview.png" )
```
This function assigns the *preview image* that will be displayed for the <%= @meta_class %> when you are selecting them.

#### Function Call
`meta:set_preview_texture( path )`

#### Parameters
**`string`** `path`<br>
&emsp;_The **relative path** from the executable to the file._
