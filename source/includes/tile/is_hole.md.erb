### Tile : is_hole()
``` lua
local tile 	-- example Tile
local is_hole = tile:is_hole()
```

Returns `true` if the **`Tile`**'s current <%= @link_tile_state %> is *not* `Broken`, `Empty`, or `Hidden`.<br>
Otherwise, returns `false`.

#### Function Call
`tile:is_hole()`

#### Return Value
**`boolean`**
