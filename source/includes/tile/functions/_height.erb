<% @current_function =
    {
    	version: "2.0",
        function_name: "height",
        lua_code:
"local tile     -- example Tile
local height = tile:height()",
        description: "Returns the <b>height</b> of the " + @link_tile + " (in pixels).",
        return_value_type: "number",
        return_value_desc: "The <b>height</b> of the " + @link_tile + ", in pixels."
    }
%>
