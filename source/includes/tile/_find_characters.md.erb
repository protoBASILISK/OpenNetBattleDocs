### Tile : find_characters()
``` lua
local function search_function( character )
	return character:get_team() == Team.Blue and character:get_health() >= 500
end

local character_list = tile:find_characters( search_function )
```
> The above snippet finds all the **Characters** on this **Tile** that are on the **Blue Team** with at least **500** health.

This function iterates through all of the <%= @link_character %>s on this <%= @link_tile %>, and returns a list of them that matches a specific criteria *(provided by a function)*.

#### Function Call
`tile:find_tiles( search_function )`

#### Return Value
<%= @link_character %> **`vector`**

#### Parameters
**`function`** `search_function`<br>
	&emsp;_A function that filters the list of <%= @link_character %>s._<br>

``` lua
local function search_function( character )
  return character:get_team() == Team.Red
end
```

<aside class="notice"><code>search_function</code> requires <b>one</b> <%= @link_character %> parameter and to return a <b><code>boolean</code></b>.<br>
Any <%= @link_character %> that causes the function to return <code>true</code> will be added to the list.</aside>
