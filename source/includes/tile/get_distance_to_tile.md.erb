### Tile : get_distance_to_tile()
``` lua
local tile 		-- example Tile
local other_tile 	-- example Tile
local distance = tile:get_distance_to_tile( other_tile )
```

Returns the [Manhattan distance](https://en.wikipedia.org/wiki/Taxicab_geometry) between this <%= @link_tile %> and another.

#### Function Call
`tile:get_distance_to_tile( other )`

#### Return Value
**`integer`**

#### Parameters
<%= @link_tile %> `other`<br>
	&emsp;_The <%= @link_tile %> you want to measure the distance to._
