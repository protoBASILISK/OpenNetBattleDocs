### Tile : remove_entity_by_id()
``` lua
local tile 	-- example Tile
local entity_id	-- example Entity ID

tile:remove_entity_by_id( entity_id )
```

Causes the <%= @link_tile %> to remove any <%= @link_entity %> present on it with a specific **unique ID**.

Returns `true` if an <%= @link_entity %> was removed.<br>
Otherwise, returns `false`.

#### Function Call
`tile:remove_entity_by_id( entity_id )`

#### Parameters
**`integer`** `entity_id`<br>
	&emsp;_The **unique ID** of the <%= @link_entity %> that you want to remove from this <%= @link_tile %>._

#### Return Value
**`boolean`**
