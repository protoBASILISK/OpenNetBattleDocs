### drag()
``` lua
local drag_amount = drag( Direction.Left, 1 )
```
> The above snippet creates a **Drag** object that will cause an attack to move an **Entity** hit by one space to the left.

Creates a <%= @link_drag %> object, that when used, will cause an attack to move any <%= @link_entity %> hit.

#### Function Call
`drag( direction, distance )`

#### Return Value
<%= @link_drag %>

#### Parameters
<%= @link_direction %> `direction`<br>
&emsp;_The <%= @link_direction %> you want any <%= @link_entity %> hit to be moved in._

**`integer`** `distance`<br>
&emsp;_The **distance** (in tiles) you want any <%= @link_entity %> hit to be moved._
