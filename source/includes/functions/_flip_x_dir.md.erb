### flip_x_dir()
``` lua
return flip_x_dir( Direction.UpLeft )
```
> The above code snippet returns **Direction.UpRight**.

Obtains a <%= @link_direction %> that has the same **vertical** component as <%= @link_direction %>, but the **opposite horizontal** component.

#### Function Call
`reverse_dir( direction )`

#### Parameters
<%= @link_direction %> `direction`<br>
&emsp;_The <%= @link_direction %> you want to reverse the **horizontal** component of._

#### Return Value
<%= @link_direction %>
