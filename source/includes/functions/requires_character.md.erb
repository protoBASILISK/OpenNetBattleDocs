### requires_character()
``` lua
requires_character( "com.example.character.required" )
```

This **`function`** raises an error if no <%= @link_character %> exists with the provided **package name**.

#### Function Call
`reverse_dir( package_name )`

#### Parameters
**`string`** `package_name`<br>
&emsp;_The **`package name`** of the required mod file._
