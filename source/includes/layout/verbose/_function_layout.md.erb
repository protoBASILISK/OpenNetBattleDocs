<% _func = locals[:f] %>
<% _class = locals[:c] %>

### <%= _class["class_name"] %> : <%= _func["name"] %>()
<h4 class="version">function • v<%= _func["version"] %></h4>

    <%# VERSION INFORMATION %>
<% if _func["incomplete"].present? or is_empty( _func["description"] ) or is_empty( _func["example_code"] ) %>
    <%# <p class= "data incomplete" > The data for this function is not fully filled out yet. </p> %>
<% else %>
    <p class= "data" > This data for this function is up-to-date, but may be missing some information on uses/restrictions. </p>
<% end %>

    <%# FUNCTION CALL %>
#### Function Call
<code><%= _class["variable_text"] %>:<%= _func["name"] %>(
    <%- if not is_empty( _func["parameters"] ) %>
        <%# For all but the last in the parameter list, write the name + a comma after. %>
        <% _func["parameters"][0...-1].each do |param| %>
            <%= param["name"] %>,
        <% end %>
        <%# For the last in the parameter list, just write the name, no comma. %>
        <%= _func["parameters"].last["name"] %>
    <% end -%>
)</code>

    <%# RETURN VALUE %>
<% if not is_empty( _func["return_value_type"] ) %>
    <%# Header has to be at the start of the line, unfortunately. %>
#### Return Value
    <b><code><%= _func["return_value_type"] %></code></b>
    <% if _func["return_value_desc"].present? %>
        <br>&emsp;<i><%= _func["return_value_desc"] %></i>
    <% end %>
<% end %>

    <%# PARAMETERS %>
<% if not is_empty( _func["parameters"] ) %>
    <%# Header has to be at the start of the line, unfortunately. %>
#### Parameters  
    <%# Print out the information on each of the parameters: Type, name, then description. %>
    <% _func["parameters"][0...-1].each do |param| %>
        <%# For all but the last one, add in a line break afterwards. %>
        <b><code><%= param["type"] %></code></b>  <code><%= param["name"] %></code><br>
        &emsp;<i><%= param["desc"] %></i><br>
    <% end %>
    <% _last = _func["parameters"].last %> 
    <%# For the last in the parameter list, just write the name, no line break. %>
    <b><code><%= _last["type"] %></code></b> <code><%= _last["name"] %></code><br>
    &emsp;<i><%= _last["desc"] %></i>
<% end %>

#### Description
    <%# LUA CODE %>
    <%# Unfortunately, these all HAVE to be at the start of the line or it'll cause problems for the parser. %>
<% if not is_empty( _func["example_code"] ) %>
``` lua
<% _func["example_code"].each do |line| %>
<%= line %>
<% end %>
```
<%= "> " + _func["example_comment"] if _func["example_comment"].present? %>
<% end %>

    <%# DESCRIPTION %>
<% if not is_empty( _func["description"] ) %>
    <% if _func["description"].respond_to? :each %>

        <% _func["description"].each do |line| %>
            <%= ERB.new( line ).result_with_hash( $link_list ) %><br>
        <% end %>
    <% elsif _func["description"].is_a? String %>
        <%= _func["description"] %>
    <% end %>
<% end %>

    <%# FOOT NOTES %>
<% if not is_empty( _func["footnotes"] ) %>
    <% _func["footnotes"].each do |note| %>
        <% if note["code"].present? %>
``` lua
            <% note["code"].each do |line| %>
<%= line %>
            <% end %>
```
        <% end %>
        <% if note["comment"].present? %>
> <%= note["comment"] %>
        <% end %>
        
        <aside class= <%= note["type"] %> >
            <% note["text"].each do |line| %>
                <%= line %><br>
            <% end %>
        </aside>
        
    <% end %>
<% end %>