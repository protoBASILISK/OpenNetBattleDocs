## Animation

**`Animation`** objects are in charge of animating sprites, separated from the sprite objects themselves.<br>
They are in charge of defining animation states, as well as any logic that gets triggered during animations.

<%= partial "includes/layout/print_type_info.md.erb", locals: { file: "/data/classes/animation.json" } %>

### Playback Modes

The `Playback` enum describes a number of ways that animations can be played.

The default mode is `Playback.Once`.

<%= partial "includes/layout/print_type_info.md.erb", locals: { file: "/data/enums/playback.json" } %>