``` lua
local function search_function( <%= locals[:variable] %> )
  return character:get_team() == Team.Red
end
```

<aside class="notice"><code>search_function</code> requires <b>one</b> <%= locals[:link] %> parameter and to return a <b><code>boolean</code></b>.<br>
The <code><%= locals[:variable] %></code> parameter is the <%= locals[:link] %> that is being checked at the time it is called.<br>
Any <%= locals[:link] %> that causes the function to return <code>true</code> will be added to the list.</aside>
