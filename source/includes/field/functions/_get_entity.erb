<% @current_function =
    {
    	version: "2.0",
        function_name: "get_entity",
        lua_code:
"",
        lua_comment: "",
        description: "This function returns an " + @link_entity + " with a specific ID.<br>" +
                        "If there is no " + @link_entity + " with the provided ID, `nil` is returned instead.",
        parameters: [
            {
                name: "unique_id",
                desc: "The ID of the " + @link_entity + " you want to retrieve.",
                type: "number"
            }
        ],
        return_value_type: @link_entity,
        return_value_desc: "",
        footnotes: "",
        footnote_code: "",
    }
%>
