### Field : spawn()
```lua
local new_enemy_1	-- example entity
local new_enemy_2	-- example entity
local new_enemy_3	-- example entity

local spawn1 = field:spawn( new_enemy_1, 5, 1 )
local spawn2 = field:spawn( new_enemy_2, 5, 2 )
local spawn3 = field:spawn( new_enemy_3, 5, 3 )
```
> The above snippet spawns three enemies on the center column of the opponent's area.

Attempts to spawn a <%= @link_entity %> on the battlefield, at a specified position.<br>
Returns `EntityStatus.Added` if it was successfully added to the field.<br>
Returns `EntityStatus.Queued` if it will attempt to spawn the <%= @link_entity %> _at the end of the frame._<br>

If you attempt to spawn an <%= @link_entity %> outside of the bounds of the battlefield, it will <i>not</i> spawn at all.<br>
(i.e. <code>x < 0</code>, <code>y < 0</code>, <code>(x > field:width() - 1)</code> or <code>y > (field:height() - 1)</code>)<br>
It will return <code>AddEntityStatus.Deleted</code> if that happens.

#### Function Call
`field:spawn( entity, x, y )`

#### Return Value
<%= @link_entity_status %>

#### Parameters
<%= @link_entity %> `entity`<br>
	&emsp;_A pointer to the <%= @link_entity %> you want to spawn._<br>
**`integer`** `x`<br>
	&emsp;_The **column** you want to spawn the <%= @link_entity %> in **(from left to right)**._<br>
**`integer`** `y`<br>
	&emsp;_The **row** you want to spawn the <%= @link_entity %> in **(from top to bottom)**._

<aside class="notice">See the <%= partial "link.erb", locals: {target: "api/#field", text: "diagram", bold: false, code: false} %> at the top for a guide on valid spawn locations.</aside>
