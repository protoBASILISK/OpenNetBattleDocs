### Field : find_tiles()
``` lua
local function search_function( tile )
	return tile:get_team() == Team.Blue
end

local tile_list = field:find_tiles( search_function )
```
> The above snippet finds all the **Tiles** that are currently owned by the **Blue Team**.

This function iterates through all of the <%= @link_tile %> objects on the battlefield, and returns a list of them that matches a specific criteria *(provided by a function)*.

#### Function Call
`field:find_tiles( search_function )`

#### Return Value
<%= @link_tile %> **`vector`**

#### Parameters
**`function`** `search_function`<br>
	&emsp;_A function that determines if the <%= @link_tile %> gets added to the list._

<%= partial "includes/field/partial/search_function.md.erb", locals: { link: @link_tile, variable: "tile" } %>
