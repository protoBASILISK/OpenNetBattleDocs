### Field : get_character()
``` lua
local example_id	-- The ID of an example character
local example_character = field:get_character( character_id )
```

This function returns a **`pointer`** to a <%= @link_character %> with a specific ID.

If there is no <%= @link_character %> with the provided ID, `nil` is returned instead.<br>
This also happens if there is an <% @link_entity %> with the provided ID, but is *not* a <%= @link_character %> (e.g. is an <% @link_obstacle %>, etc.)

#### Function Call
`field:get_character( character_id )`

#### Return Value
<%= @link_character %>

#### Parameters
**`integer`** `character_id`<br>
	&emsp;_The ID of the <%= @link_character %> you want to retrieve._
