{
    "version": "2.0",
    "class_name": "Lifetimes",
    "items":
    [
        {
            "name":     "Local",
            "meaning":  [ 
                "This <%= type 'Component' %>'s `update()` loop happens following the associated <%= type 'Entity' %>'s `update()`." 
            ]
        },
        {
            "name":     "Battlestep",
            "meaning":  [ 
                "This <%= type 'Component' %>'s `update()` loop happens every **battle scene** `update()`."
            ]
        },
        {
            "name":     "Scene",
            "meaning":  [
                "This <%= type 'Component' %>'s `update()` loop happens every **application** `update()`."
            ]
        }
    ] 
}