
{
	"version": "2.0",
	"name": "share_tile",
	"example_code": [
		"local <%= variable_text %>\t-- example <%= variable_text %>",
		"<%= variable_text %>:share_tile( true ) "
	],
    "summary": "",
    "description": 
	[
		"If `true`, allows other objects to inhabit the same <%= type 'Tile' %> as this <%= doc_class_name %>.",
		"&emsp;(default : `false`)"
	],
	"parameters": [
		{
			"name": "share",
			"desc": "`true`, if other objects should be allowed to inhabit the same tile as this <%= doc_class_name %>.",
			"type": "boolean"
		}
	]
}