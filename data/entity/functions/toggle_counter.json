
{
	"version": "2.0",
	"name": "toggle_counter",
	"example_code": [
		"local <%= variable_text %>\t-- example <%= variable_text %>",
		"<%= variable_text %>:toggle_counter( true )"
	],
    "summary": "",
    "description": 
	[
		"If `true`, forces this <%= doc_class_name %> to **enter** a \"counter-able\" state.",
		"&emsp;Otherwise, forces it to **leave** the \"counter-able\" state.",
		"**TODO** : Nail down exactly what happens when hit by counters."
	],
	"parameters": [
		{
			"name": "counterable",
			"desc": "`true`, if this <%= doc_class_name %> should be able to be counter-hit. Otherwise `false`.",
			"type": "boolean"
		}
	]
}