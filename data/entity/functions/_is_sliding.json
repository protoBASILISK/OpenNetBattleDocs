
{
	"version": "2.0",
	"function_name": "is_sliding",
	"lua_code":
	[
		"local <%=variable_text%>\t-- example <%=variable_text%>",
		"local is_sliding = <%=variable_text%>:is_sliding()"
	],
	"description": 
	[
		"Returns `true` if this <%=class_name%> is currently sliding.",
		"&emsp;Otherwise, returns `false`."
	],
	"return_value_type": "boolean",
	"return_value_desc": "Whether this <%=class_name%> is sliding or not."
}