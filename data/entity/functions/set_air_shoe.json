
{
	"version": "2.0",
	"conditional": true,
	"name": "set_air_shoe",
	"example_code": [
		"local <%=variable_text%>\t-- example <%=variable_text%>",
		"<%=variable_text%>:set_air_shoe( true ) "
	],
	"description": [
		"If true, puts whether this <%=doc_class_name%> has the effects of `AirShoe`.",
		"Otherwise, removes the effect of `AirShoe` if the <%=doc_class_name%> has it.",
		"While under the effects of `AirShoe`, the <%=doc_class_name%> is able to move over `Broken` <%=tile%>s."
	],
	"parameters": [
		{
			"name": "has_effect",
			"desc": "`true` if you want this <%=doc_class_name%> to have the `AirShoe` effect. Otherwise `false`.",
			"type": "boolean"
		}
	]
}