
{
	"version": "2.0",
	"name": "never_flip",
	"example_code": [
		"local <%= variable_text %>\t-- example <%= variable_text %>",
		"<%= variable_text %>:never_flip( true ) "
	],
    "summary": "",
    "description": 
	[
		"If `true`, stops this <%= doc_class_name %> from flipping direction (graphically) based on its <%= type 'Team'%>."
	],
	"parameters": [
		{
			"name": "disable_flip",
			"desc": "`true`, if this <%= doc_class_name %> should not flip direction (graphically).",
			"type": "boolean"
		}
	]
}