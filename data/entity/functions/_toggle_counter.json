
{
	"version": "2.0",
	"conditional": true,
	"function_name": "toggle_counter",
	"lua_code":
	[
		"local <%=variable_text%>\t-- example <%=variable_text%>",
		"<%=variable_text%>:toggle_counter( true )" 
	],
	"description": [
		"If `true`, forces this <%=class_name%> to **enter** a \"counter-able\" state.",
		"&emsp;Otherwise, forces it to **leave** the \"counter-able\" state.",
		"**TODO** : Nail down exactly what happens when hit by counters."
	],
	"parameters": [
		{
			"name": "counterable",
			"desc": "`true`, if this <%=class_name%> should be able to be counter-hit. Otherwise `false`.",
			"type": "boolean"
		}
	]
}