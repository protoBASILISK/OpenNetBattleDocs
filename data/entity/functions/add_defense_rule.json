
{
	"version": "2.0",
	"name": "add_defense_rule",
	"example_code": [ 
		"local <%= variable_text %>\t-- example <%= variable_text %>",
		"local example_rule  -- example DefenseRule",
		"<%= variable_text %>:add_defense_rule( example_rule )"
	],
    "summary": "",
    "description": [
		"Adds a <%= type 'DefenseRule' %> to a(n) <%= doc_class_name %>.",
		"<%= type 'DefenseRule' %>s allow the <%= doc_class_name %> to change/ignore certain properties of attacks that affect them.",
		"**TODO** : Get an actually good explanation of what they do."
	],
	"parameters": [
		{
			"name": "defense_rule",
			"desc": "**TODO**",
			"type": "<%= type 'DefenseRule' %>"
		}
	]
}